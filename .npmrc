# Hoisted node_modules keeps Next.js + native modules (better-sqlite3) resolving
# cleanly across the monorepo. See packages/db for the native dependency.
node-linker=hoisted
strict-peer-dependencies=false
